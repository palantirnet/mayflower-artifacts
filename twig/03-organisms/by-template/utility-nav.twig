<div class="ma__utility-nav js-util-nav">
  <ul class="ma__utility-nav__items">
    {% if googleLanguages %}
      <li class="ma__utility-nav__item">
        <div class="ma__utility-nav__translate">
          <div id="google_translate_element"></div>
          <div class="ma__utility-nav__translate-icon">
            {{ icon('lat-lon-globe')}}
          </div>
        </div>
      </li>
    {% endif %}
    {% for item in utilityNav.items %}
      <li class="ma__utility-nav__item js-util-nav-toggle">
        <button id="utilityNav_{{ item.id }}" class="ma__utility-nav__link"
          href="#"
          aria-label="{% if item.ariaLabelText %}{{ item.ariaLabelText }}{% else %}{{ item.text }}{% endif %}">
          {{ icon(item.icon) }}
          <span>
            {{ item.text }}
          </span>
        </button>
      </li>
    {% endfor %}
  </ul>
</div>
